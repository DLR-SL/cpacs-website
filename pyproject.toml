[project]
name = "cpacs-website"
version = "0.0.0"
description = "Reproducible build environment for the CPACS website"
requires-python = "==3.13.*"
dependencies = [
    "pelican[markdown]==4.12.0",
]

[tool.uv]
package = false
required-version = ">=0.11.29,<0.12"
exclude-newer = "2026-07-30T00:00:00Z"
